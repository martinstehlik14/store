# PostgreSQL connection string (matches docker-compose.yml)
DATABASE_URL="postgresql://store:store_dev_password@localhost:5432/store?schema=public"

# API port (default 3000)
PORT=3000

# JWT signing secret - use a long random string in production!
JWT_SECRET=dev_secret_change_me_1234567890
